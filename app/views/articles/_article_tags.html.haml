-if article.tag_list.any?
  .article-tags
    %strong Теги:
    %ul.inline
      -article.tag_list.each do |tag|
        %li{ class: "badge badge-info" }= link_to tag, tagged_with_path(tag), class: "article-tag"