<p><%=t 'devise.mailer.hello', email: @resource.email %></p>

<p><%=t 'devise.mailer.reset.body' %></p>

<p><%= link_to t('devise.mailer.reset.link'), edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>

<%= t('devise.mailer.reset.footer').html_safe %>
